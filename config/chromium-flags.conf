# Follow the system dark preference for Chromium's own UI (toolbar,
# tabs, settings pages) instead of its light-by-default theme. Read
# by the Arch chromium launcher wrapper on every start.
--force-dark-mode
--enable-features=WebUIDarkMode
